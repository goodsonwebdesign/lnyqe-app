import { Injectable } from '@angular/core';

/**
 * Simple service for managing popup components in the application
 */
@Injectable({
  providedIn: 'root'
})
export class PopupService {
  // This service is kept as a minimal implementation
  // Specific popup functionality is delegated to dedicated services
}
