/**
 * UI Components Collection
 *
 * This barrel file re-exports all UI components from the index folder.
 * This creates a cleaner import path for components:
 *
 * import { IconComponent, ButtonComponent, UI_COMPONENTS } from '../../shared/components/ui';
 */

export * from './index/index';
